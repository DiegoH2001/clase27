const process = require('process');
const {argv}=process


function calculadora(comando) {
    

switch (comando) {
    case "sumar":
        
        break;
    case "restar":
        break;
    case "multiplicar":
        break
    default:
    case"dividir":
        break;
}
}